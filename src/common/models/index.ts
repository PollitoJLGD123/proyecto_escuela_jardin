import { Alumno } from '../../modules/alumnos';
import { Docente } from '../../modules/docentes';

const modelsList = [
    Alumno,
    Docente,
];

const modelsDict = {
    Alumno,
    Docente,
}

export { modelsList, modelsDict };